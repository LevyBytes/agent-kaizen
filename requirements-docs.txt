# OPT-IN doc-ingestion backends: activate .pdf / .docx / .xlsx support for E1 (evidence
# ingestion). NEVER referenced by requirements-kaizen.txt — the dependency-light core stays
# stdlib + pyturso and the native readers (.txt/.md/.html/.csv) work with no install.
#
#     pip install -r requirements-docs.txt
#
# Version floors, not exact pins: pypdf ships frequent security fixes (malformed-PDF DoS /
# excessive-memory classes), so keep it recent — bump these to the latest releases at release
# time. E1 also enforces size/page/encrypted/empty-text guards regardless of pypdf version.
pypdf>=5.1.0
python-docx>=1.1.2
openpyxl>=3.1.5
